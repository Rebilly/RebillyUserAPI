parameters:
  - $ref: "#/parameters/resourceId"
post:
  tags:
    - Email Templates
  summary: Replace email template's placeholders with real data
  description: |
    Replace email template's placeholders with real data
  parameters:
    - name: body
      in: body
      description: The data context resource
      required: true
      schema:
        type: object
        required:
          - data
        properties:
          data:
            description: The data context
            type: object
  responses:
    200:
      description: Email template's placeholders was replaced
      headers:
        Rate-Limit-Limit:
          $ref: "#/headers/Rate-Limit-Limit"
        Rate-Limit-Remaining:
          $ref: "#/headers/Rate-Limit-Remaining"
        Rate-Limit-Reset:
          $ref: "#/headers/Rate-Limit-Reset"
      schema:
        $ref: "#/definitions/EmailTemplate"
