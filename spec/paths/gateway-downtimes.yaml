get:
  tags:
    - Gateway Accounts
  summary: Retrieve a list of gateway downtimes
  description: |
    Retrieve a list of gateway downtimes
  parameters:
    - $ref: "#/parameters/collectionLimit"
    - $ref: "#/parameters/collectionOffset"
  responses:
    200:
      description: A list of Gateway downtimes was retrieved successfully
      headers:
        Rate-Limit-Limit:
          $ref: "#/headers/Rate-Limit-Limit"
        Rate-Limit-Remaining:
          $ref: "#/headers/Rate-Limit-Remaining"
        Rate-Limit-Reset:
          $ref: "#/headers/Rate-Limit-Reset"
        Pagination-Total:
          $ref: "#/headers/Pagination-Total"
        Pagination-Limit:
          $ref: "#/headers/Pagination-Limit"
        Pagination-Offset:
          $ref: "#/headers/Pagination-Offset"
      schema:
        type: array
        items:
          $ref: "#/definitions/GatewayDowntime"
    401:
      $ref: "#/responses/AccessForbidden"
post:
  tags:
    - Gateway Accounts
  summary: Create a Gateway Downtime
  description: |
    Create a Gateway Downtime
  parameters:
    - name: body
      in: body
      description: Gateway Downtime resource
      required: true
      schema:
        $ref: "#/definitions/GatewayDowntime"
  responses:
    201:
      description: Gateway Downtime was created
      headers:
        Rate-Limit-Limit:
          $ref: "#/headers/Rate-Limit-Limit"
        Rate-Limit-Remaining:
          $ref: "#/headers/Rate-Limit-Remaining"
        Rate-Limit-Reset:
          $ref: "#/headers/Rate-Limit-Reset"
      schema:
        $ref: "#/definitions/GatewayDowntime"
    401:
      $ref: "#/responses/AccessForbidden"
    422:
      description: Invalid data was sent
      schema:
        $ref: "#/definitions/InvalidError"
