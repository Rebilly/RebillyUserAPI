type: object
description: "Schedule Instructions Collection"
properties:
  instructions:
    type: array
    items:
      $ref: "#/components/schemas/ReminderScheduleInstruction"
  chronology:
    type: string
    enum:
      - "before"
      - "after"
required:
  - instructions
  - chronology
