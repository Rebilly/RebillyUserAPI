description: ""
allOf:
  - $ref: "#/components/schemas/RuleAction"
  - type: object
    properties:
      role:
        type: string
        enum:
          - all
          - renewal
          - trial
        description: The role of Reminder (Available only on Subscription events, other should use `all`).
    required:
      - role
