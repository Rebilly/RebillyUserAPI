type: object
properties:
  hash:
    readOnly: true
    allOf:
      - $ref: "#/components/schemas/ResourceId"
  disabledTime:
    readOnly: true
    description: The time when the credential was disabled
    type: string
    format: date-time
