x-rebillyMerge:
  - $ref: "#/components/schemas/PatchCredential"
  - type: object
    properties:
      hash:
        readOnly: true
        allOf:
          - $ref: "#/components/schemas/ResourceId"
      deletedTime:
        readOnly: true
        description: The credential deletion time
        type: string
        format: date-time
