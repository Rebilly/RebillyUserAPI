type: object
properties:
  service:
    readOnly: true
    description: The integration service
    allOf:
      - $ref: "#/components/schemas/OAuth2CredentialService"
  count:
    readOnly: true
    type: integer
    minimum: 1
    description: The number of available rule sets
  configurations:
    readOnly: true
    type: array
    minItems: 1
    description: The list of configurations
    items:
      type: object
      properties:
        labels:
          description: The configuration labels
          type: array
          minItems: 1
          items:
            type: string
        eventType:
          description: The configuration event type
          allOf:
            - $ref: "#/components/parameters/systemEventType/schema"
        title:
          type: string
          description: The configuration title
  _links:
    type: array
    description: The links related to resource
    readOnly: true
    minItems: 1
    items:
      anyOf:
        - $ref: "#/components/schemas/SelfLink"
        - $ref: "#/components/schemas/OAuth2ConnectLink"
