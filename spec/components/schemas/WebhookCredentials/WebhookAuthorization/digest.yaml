allOf:
  - $ref: "#/components/schemas/WebhookAuthorization"
  - type: object
    properties:
      username:
        type: string
      password:
        type: string
        format: password
    required:
      - username
      - password
