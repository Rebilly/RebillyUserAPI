description: Gpaysafe Gateway config
allOf:
  -
    $ref: "#/components/schemas/GatewayAccount"
  -
    type: "object"
    required:
     - "credentials"
    properties:
      credentials:
        type: object
        properties:
          apiKey:
            type: string
            description: Gpaysafe apiKey
        required:
          - "apiKey"
