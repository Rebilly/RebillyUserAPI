type: object
description: The suppression email list.
required:
  - emailHash
properties:
  email:
    type: string
    format: email
    description: The email address.
  emailHash:
    type: string
    description: The email address hash.
  ipAddress:
    type: string
    description: The IP address.
  emailMessageId:
    type: string
    format: uuid
    description: The email message ID.
  createdTime:
    description: The created time.
    allOf:
      - $ref: "#/components/schemas/ServerTimestamp"
