type: object
properties:
  condition:
    $ref: "#/components/schemas/Condition"
required:
  - condition
example:
  operation: not
  condition:
    operation: equals
    path: "/name"
    value: John Dou
