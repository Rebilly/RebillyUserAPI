description: GET Mpis
discriminator:
  propertyName: name
type: object
required:
  - name
properties:
  name:
    allOf:
      - $ref: "#/components/schemas/MpiName"
    enum:
     - PaayMpi
     - Other
