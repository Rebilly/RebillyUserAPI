description: Payvision Mpis
discriminator:
  propertyName: name
type: object
required:
  - name
properties:
  name:
    allOf:
      - $ref: "#/components/schemas/MpiName"
    enum:
     - PayvisionMpi
     - Payvision3DS2
     - CardinalCommerce
     - PaayMpi
     - Other
