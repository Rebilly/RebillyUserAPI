content:
  application/json:
    schema:
      properties:
        accountId:
          $ref: '#/components/schemas/ResourceId'
        userId:
          $ref: '#/components/schemas/ResourceId'
        isOwner:
          type: boolean
description: Membership resource
required: true
