content:
  application/json:
    schema:
      properties:
        merchantId:
          $ref: '#/components/schemas/ResourceId'
        userId:
          $ref: '#/components/schemas/ResourceId'
        isOwner:
          type: boolean
description: Membership resource
required: true
