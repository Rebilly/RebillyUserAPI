type: object
properties:
  subscription-canceled:
    allOf:
      - $ref: "#/definitions/SubscriptionPlaceholders"
  subscription-activated:
    allOf:
      - $ref: "#/definitions/SubscriptionPlaceholders"
  subscription-created:
    allOf:
      - $ref: "#/definitions/SubscriptionPlaceholders"
  subscription-trial-ended:
    allOf:
      - $ref: "#/definitions/SubscriptionPlaceholders"
  subscription-ended:
    allOf:
      - $ref: "#/definitions/SubscriptionPlaceholders"
  dispute-created:
    allOf:
      - $ref: "#/definitions/DisputePlaceholders"
  gateway-account-requested:
    allOf:
      - $ref: "#/definitions/TransactionPlaceholders"
  invoice-past-due:
    allOf:
      - $ref: "#/definitions/InvoicePlaceholders"
  payment-card-expired:
    allOf:
      - $ref: "#/definitions/PaymentCardPlaceholders"
  subscription-renewed:
    allOf:
      - $ref: "#/definitions/SubscriptionPlaceholders"
    properties:
      invoice:
        allOf:
          - $ref: "#/definitions/Invoice"
      invoice/website:
        allOf:
          - $ref: "#/definitions/Website"
      invoice/customer:
        allOf:
          - $ref: "#/definitions/Customer"
      invoice/customer/customFields/DOB:
        type: string
      invoice/website/customFields/GSID:
        type: string
  transaction-processed:
    allOf:
      - $ref: "#/definitions/TransactionPlaceholders"
  payment-declined:
    allOf:
      - $ref: "#/definitions/TransactionPlaceholders"
  transaction-process-requested:
    allOf:
      - $ref: "#/definitions/TransactionPlaceholders"
  risk-score-changed:
    allOf:
      - $ref: "#/definitions/TransactionPlaceholders"
