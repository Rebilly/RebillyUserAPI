description: Jeton Gateway config
allOf:
  -
    $ref: "#/definitions/GatewayAccount"
  -
    type: "object"
    required:
     - "gatewayConfig"
    properties:
      gatewayConfig:
        type: object
        properties:
          apiKey:
            type: string
            description: Jeton apiKey
        required:
          - "apiKey"
