description: Jeton Gateway config
allOf:
  -
    $ref: "#/definitions/GatewayAccount"
  -
    type: "object"
    required:
     - "credentials"
    properties:
      credentials:
        type: object
        properties:
          apiKey:
            type: string
            description: Jeton apiKey
        required:
          - "apiKey"
