description: SMSVoucher Gateway config
allOf:
  -
    $ref: "#/definitions/GatewayAccount"
  -
    type: "object"
    required:
     - "credentials"
    properties:
      credentials:
        type: object
        properties:
          AppId:
            type: string
            description: SMSVoucher AppId
        required:
          - "AppId"
