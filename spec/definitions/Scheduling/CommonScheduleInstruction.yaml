type: object
description: "The calculation instruction of scheduled time"
discriminator: method
properties:
  method:
    type: string
    enum:
      - "auto"
      - "intelligent"
      - "immediately"
      - "date-interval"
      - "day-of-month"
      - "day-of-week"
required:
  - method
