description: Cancel Redemption
allOf:
  -
    $ref: "#/definitions/TimelineAction"
  -
    type: object
    properties:
      redemptionId:
        allOf:
          - $ref: "#/definitions/ResourceId"
