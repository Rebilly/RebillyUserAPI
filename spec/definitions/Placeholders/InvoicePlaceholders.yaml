type: object
properties:
  invoice:
    allOf:
      - $ref: "#/definitions/Invoice"
  invoice/customer:
    allOf:
      - $ref: "#/definitions/Customer"
  invoice/website:
    allOf:
      - $ref: "#/definitions/Website"
