type: object
properties:
  root:
    type: string
    default: 'transaction'
  transaction:
    allOf:
      - $ref: "#/definitions/Transaction"
  gatewayAccount:
    allOf:
      - $ref: "#/definitions/GatewayAccount"
  bankAccount:
    allOf:
      - $ref: "#/definitions/BankAccount"
  website:
    allOf:
      - $ref: "#/definitions/Website"
  customer:
    allOf:
      - $ref: "#/definitions/Customer"
  paymentCard:
    allOf:
      - $ref: "#/definitions/PaymentCard"
  customFields.NotificationURL:
    type: string
  customFields.gatewayAccount.NotificationURL:
    type: string
  customer.customFields.DOB:
    type: string
  website.customFields.GSID:
    type: string
  paymentCard.customFields.CardID:
    type: string