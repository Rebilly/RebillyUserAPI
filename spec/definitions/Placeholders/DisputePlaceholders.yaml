type: object
properties:
  dispute:
    allOf:
      - $ref: "#/definitions/Dispute"
  dispute/transaction:
    allOf:
      - $ref: "#/definitions/Transaction"
  dispute/transaction/website:
    allOf:
      - $ref: "#/definitions/Website"
  dispute/transaction/customer:
    allOf:
      - $ref: "#/definitions/Customer"
  dispute/transaction/paymentCard:
    allOf:
      - $ref: "#/definitions/PaymentCard"
  dispute/transaction/customFields/NotificationURL:
    type: string
  dispute/transaction/customer/customFields/DOB:
    type: string
  dispute/transaction/website/customFields/GSID:
    type: string
  dispute/transaction/paymentCard/customFields/CardID:
    type: string
