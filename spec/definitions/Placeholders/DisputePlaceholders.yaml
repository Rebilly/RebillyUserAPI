type: object
properties:
  root:
    type: string
    default: 'dispute'
  dispute:
    allOf:
      - $ref: "#/definitions/Dispute"
  transaction:
    allOf:
      - $ref: "#/definitions/Transaction"
  transaction.website:
    allOf:
      - $ref: "#/definitions/Website"
  transaction.customer:
    allOf:
      - $ref: "#/definitions/Customer"
  transaction.paymentCard:
    allOf:
      - $ref: "#/definitions/PaymentCard"
  transaction.customFields.NotificationURL:
    type: string
  transaction.customer.customFields.DOB:
    type: string
  transaction.website.customFields.GSID:
    type: string
  transaction.paymentCard.customFields.CardID:
    type: string