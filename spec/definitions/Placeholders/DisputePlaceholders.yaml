type: object
properties:
  dispute:
    allOf:
      - $ref: "#/definitions/Dispute"
  dispute/transaction:
    allOf:
      - $ref: "#/definitions/Transaction"
  dispute/transaction/website:
    allOf:
      - $ref: "#/definitions/Website"
  dispute/transaction/customer:
    allOf:
      - $ref: "#/definitions/Customer"
  dispute/transaction/paymentCard:
    allOf:
      - $ref: "#/definitions/PaymentCard"
