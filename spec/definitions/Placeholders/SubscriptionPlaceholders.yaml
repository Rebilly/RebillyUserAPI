type: object
properties:
  subscription:
    $ref: "#/definitions/Subscription"
  subscription/plan:
    $ref: "#/definitions/Plan"
  subscription/website:
    $ref: "#/definitions/Website"
  subscription/customer:
    $ref: "#/definitions/Customer"
  subscription/initialInvoice:
    $ref: "#/definitions/Invoice"
