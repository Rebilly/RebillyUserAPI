type: object
properties:
  root:
    type: string
    default: 'subscription'
  subscription:
    allOf:
      - $ref: "#/definitions/Subscription"
  plan:
    allOf:
      - $ref: "#/definitions/Plan"
  website:
    allOf:
      - $ref: "#/definitions/Website"
  customer:
    allOf:
      - $ref: "#/definitions/Customer"
  customFields.users:
    type: array
  customer.customFields.DOB:
    type: string
  website.customFields.GSID:
    type: string