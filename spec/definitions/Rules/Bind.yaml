type: object
description: The rule
properties:
  name:
    type: string
  status:
    $ref: "#/definitions/OnOff"
  criteria:
    $ref: "#/definitions/Condition"
  actions:
    type: array
    uniqueItems: true
    items:
      $ref: "#/definitions/RuleAction"
required:
  - name
  - actions
