type: object
required:
- code
- state
- scope
properties:
  code:
    description: The code retrieved from OAuth2 server
    type: string
  state:
    description: The state retrieved from OAuth2 server
    type: string
  scope:
    description: The scope retrieved from OAuth2 server
    type: array
    items:
      type: string
